const und = require('underscore');
const lod = require('lodash');

/**
 * slice - возвращает часть массива
 */

// lodash
console.log("lod.slice([1, 2, 3, 4], 1, 2)", lod.slice([1, 2, 3, 4], 1, 2));

// es6
console.log("[1, 2, 3, 4].slice(1, 2)", [1, 2, 3, 4].slice(1, 2));
