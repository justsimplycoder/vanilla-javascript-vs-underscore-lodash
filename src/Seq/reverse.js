const und = require('underscore');
const lod = require('lodash');

/**
 * reverse - изменяет порядок следования элементов в массиве на обратный
 */

// lodash
console.log("lod([1, 2, 3]).reverse().value()", lod([1, 2, 3]).reverse().value());

// es6
console.log("[1, 2, 3].reverse()", [1, 2, 3].reverse());