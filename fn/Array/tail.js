const und = require('underscore');
const lod = require('lodash');

/**
 * tail - Получает все, кроме первого элемента массива.
 */

// lodash
console.log("lod.tail([1, 2, 3])", lod.tail([1, 2, 3]));

// es6
console.log("[1, 2, 3].slice(1)", [1, 2, 3].slice(1));
